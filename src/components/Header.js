import React from 'react';
import {Text, View} from 'react-native';
import Header from 'react-native-elements';

const HeaderXWalker = props => (
  <Header
    placement="left"
    leftcomponent={{ icon: require() }}
  />
);

export default HeaderXWalker;
